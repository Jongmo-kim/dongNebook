<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pro">
	<select id="proposalList" parameterType="hashMap" resultType="pro">
		select 
			user_no as userNo,
			book_name as bookName,
			book_kind as bookKind,
			book_writer as bookWriter,
			book_publisher as bookPublisher,
			book_introduce as bookIntroduce,
			isbn,
			imageurl,
			reason
		from (select rownum as pNum, p.* from (select * from book_proposal order by 1 desc)p) where pNum between ${start} and ${end}
	</select>
	<select id="totalCount" resultType="_int">
		select count(*) from book_proposal
	</select>
	
	<insert id="inserBook" parameterType="book">
		insert into Book values(
			book_seq.nextval,
			#{bookName},
			#{bookKind},
			#{bookWriter},
			#{bookPublisher},
			#{bookIntroduce},
			#{ISBN},
			#{bookCount},
			#{rCount},
			#{imageurl}
		)
	</insert>
	<delete id="deleteBook" parameterType="book">
		delete from book_proposal where isbn = #{ISBN}
	</delete>
</mapper>
